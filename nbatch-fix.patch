diff --git a/native/llama_wrapper/llama_wrapper.cpp b/native/llama_wrapper/llama_wrapper.cpp
index 8f5dcb2..e9f97a9 100644
--- a/native/llama_wrapper/llama_wrapper.cpp
+++ b/native/llama_wrapper/llama_wrapper.cpp
@@ -82,7 +82,7 @@ static bool reset_llama_context(NezumiLlamaState *state)
 
     auto cparams = llama_context_default_params();
     cparams.n_ctx = static_cast<uint32_t>(state->n_ctx > 0 ? state->n_ctx : 2048);
-    cparams.n_batch = 512;
+    cparams.n_batch = cparams.n_ctx;
 
     state->ctx = llama_init_from_model(state->model, cparams);
     return state->ctx != nullptr;
